section.logo-branding#work
	h3 Projects

	.flex.logo-wrap
		{% for post in site.posts  | limit: 6%}

		a.logo-thumb(href='{{post.url}}' style='background-image: url(assets/images/logos/{{post.thumbnail}})')
			.logo-meta
				.name {{post.title}}
				.date {{post.date | date: "%b %d, %Y"}}

		{% endfor %}
	.link_button
		a(href="/toc") View all projects
